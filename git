Quick setup — if you’ve done this kind of thing before
or

We recommend every repository include a README, LICENSE, and .gitignore.
…or create a new repository on the command line

echo "# pictures" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/gkdangal/pictures.git
git push -u origin master

…or push an existing repository from the command line

git remote add origin https://github.com/gkdangal/pictures.git
git push -u origin master

…or import code from another repository

You can initialize this repository with code from a Subversion, Mercurial, or TFS project.

##############################################################
first put your ssh key on git hub server
cat ~/.ssh/id_rsa.pub
second:
global configuration

govinda@govinda:~/Pictures/pictures$ git config --global  user.name "Govinda Dangal"
govinda@govinda:~/Pictures/pictures$ git config --global  user.email "Govinda Dangal"
govinda@govinda:~/Pictures/pictures$ git config --global  user.email "gkdangal@gmail.com"


